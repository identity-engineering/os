# templates/personal — Minimal Viable Install target

This folder will contain the exact set of files that, when present, mean "IE is installed" for a person (or a simple runtime).

Target (still empty):

- `STEM.yaml` (or .json) — the Identity Stem
- `HEADER.yaml` — the always-on entry point
- `trajectory/` — versioned State Differential + Vision Gradient log
- `registry/` — local gravitational sensor / recognized Masses

Nothing is populated until the corresponding schemas exist and the minimal interaction contract is sharp.

See `docs/next.md` and issue #1.
