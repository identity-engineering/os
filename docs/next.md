# Next: IE Praxis

## Locked (local geometry runtime)

Surface Runtime (CLI / HTTP / MCP) · Geometry Receipt + feed · Mass · Freedom
scalar · Request inbox · Jurisdiction probes · Grant list/revoke/transfer ·
Account ≠ Identity · Space-as-membrane contracts · Creation-time grants ·
**Local Space rows + multi-Identity per install** (#77) · active Identity on
all local call sites (#79) · managed multi-Identity + Space membership contract
(#80) · Space membrane export/inbound enforcement (#82) · **Local MCP owner
surface** (#84) · **Multi-Identity MCP bind** (#87).

Details live in the linked docs and closed issues (#8, #15, #18, #29, #31, #37,
#40, #44, #60, #61, #77, #79, #80, #82, #84, #87).

## Now

1. **Operational dogfood + issue hygiene** — personal install use; close/supersede
   stale Phase-0 trackers already embodied by SQLite + Surface.
2. **Distribution ops (PC / Cloudflare)** — R2 + release workflows are **already
   in the repo** (#37 closed, `docs/release.md`, `release.yml`). Remaining:
   secrets, first live tag, brew path verification — not greenfield code.

## Then

3. **Dimensional Freedom / denser Tensor** — when alloys carry real dimension IDs.
4. **#73 Frequency / Resonance** — only after denser Tensor.
5. Phase 3 auth path (#24–#26) when Pro is the lever — unlocks managed MCP (#86).

## Parked / later

- **#86 Managed / remote MCP contract** — docs-first; blocked on Phase-3 auth.
- TIM foundation (#45), multi-substrate narrative (#11), public /os surface (#21),
  Environment format contracts (#5), Open/Closed mirror (#22–#23).
- Phase 4–5 product freeze (#27–#28).

Stale Phase 0/1 trackers closed when superseded: #1, #3, #6, #14, #15, #18, #19,
#20, #29, #37.
