# Next: IE Praxis

## Locked (local geometry runtime)

Surface Runtime (CLI / HTTP / MCP) · Geometry Receipt + feed · Mass · Freedom
scalar · Request inbox · Jurisdiction probes · Grant list/revoke/transfer ·
Account ≠ Identity · Space-as-membrane contracts · Creation-time grants ·
**Local Space rows + multi-Identity per install** (#77) · active Identity on
all local call sites (#79) · managed multi-Identity + Space membership contract
(#80) · Space membrane export/inbound enforcement (#82) · **Local MCP owner
surface** (#84) · multi-Identity MCP bind in progress (#87).

Details live in the linked docs and closed issues (#8, #15, #29, #31, #40, #44,
#60, #61, #77, #79, #80, #82, #84).

## Now

1. **#87 Multi-Identity MCP bind** — optional `--identity-id` / `--handle` on
   local MCP session + mcp-config pin (session-scoped, no silent elevation).

## Then

2. **Distribution** — Cloudflare R2 + install channels (`ie` package dogfood).
3. **Dimensional Freedom / denser Tensor** — when alloys carry real dimension IDs.
4. **#73 Frequency / Resonance** — only after denser Tensor.

## Parked / later

- **#86 Managed / remote MCP contract** — docs-first; blocked on Phase-3 auth
  (#24–#26); implementation in `os-managed`.
- TIM foundation (#45), multi-substrate narrative (#11), public /os surface (#21),
  Phase 3–5 billing/auth issues — still valid, not the next local lever.

Stale Phase 0/1 trackers for shipped work: closed (#15, #18, #20) or not_planned
where YAML-first was superseded by SQLite (#19).
