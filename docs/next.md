# Next: IE Praxis

Immediate work (order flexible, all high-signal).

## Locked decisions (July 2026)

- **Language:** Python core + Typer CLI (`docs/language-strategy.md`)
- Local Registry + Metric Stem + Interaction Signal contracts
- Surface Runtime v0 local apply + receipts + thin HTTP
- Bidirectional gravitational sensor design
- Ecosystem orientation — standard first, platform optional
- CLI skeleton on main (`ie init` interactive, `signal apply`, registry)

## Still open (high signal)

1. **Public release artifact** for token-free `brew install` (`docs/distribution.md`)
2. **Mass attributes & relative measurement / emergent self-Mass** (issue #15)
3. **Estimate request + inbox** (issue #31) — completes bidirectional sensor
4. **Tension Tensor update protocol** — post-interaction hooks
5. **Header / local entry** polish

Only after the relative world view and bidirectional sensor are solid do we push hard on environment adapters and any Framework-derived organism patterns.

## Orientation

- Contracts + local runtime first
- Collective-intelligence ecosystem is a possible emergence, not the v0 claim — see `docs/ecosystem-vision.md`
- Agents may use `ie`; they do not define the Identity

## Related issues

#15 (self-Mass), #18 (CLI — shipped skeleton), #29 (Surface Runtime), #31 (request/inbox), #8 (Tension), #10 (Privacy), #11 (Multi-Substrate).
